<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anelcc.simplerxjava">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".LearningRxJavaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.BasicExampleActivity" />
        <activity android:name=".activities.databaseexample.DatabaseExampleActivity" />
        <activity
            android:name=".activities.networkexample.NetworkExampleActivity"
            android:label="@string/title_activity_network_example"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.tasksexample.TasksExampleActivity" />
        <activity android:name=".activities.reactiveui.complex.ReactiveUIActivity" />
        <activity android:name=".activities.reactiveui.simple.SimpleUIActivity" />
        <activity android:name=".activities.threadingexample.ThreadingExampleActivity"></activity>
    </application>

</manifest>